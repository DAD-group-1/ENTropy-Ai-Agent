services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - reverse-proxy
    volumes:
      - ./data:/root/.ollama
    ports:
      - "11434:11434"
